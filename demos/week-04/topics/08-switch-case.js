let state = 'Maharashtra';

switch( state ) {
    case 'Punjab':
        console.log( 'Chandigarh' );
        break;
    case 'Maharashtra':
        console.log( 'Mumbai' );
        break;
    default:
        console.log( 'Unsupported state' );
}