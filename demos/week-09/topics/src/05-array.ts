import { NS } from './04-type-alias';

const nums : NS[] = [];
const primes : number[] = [];
const composites : (number | string)[] = [];

nums.push( 1 );
nums.push( 'Two' );
// nums.push( true ); // not ok

export {}